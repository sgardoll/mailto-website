---
phase: 02-core-form-config-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - setup/requirements.txt
autonomous: true
requirements: []
must_haves:
  truths:
    - "The wizard venv can import yaml after pip install -r setup/requirements.txt"
  artifacts:
    - path: "setup/requirements.txt"
      provides: "PyYAML>=6.0.1 dependency declaration"
      contains: "PyYAML>=6.0.1"
  key_links: []
---

<objective>
Add PyYAML to the wizard's requirements so that `setup/builder.py` (Wave 1) can import yaml without ImportError.

Purpose: PyYAML is already in `workflow/requirements.txt` but absent from `setup/requirements.txt`. Wave 1 builder.py cannot import yaml until this is fixed.
Output: setup/requirements.txt gains `PyYAML>=6.0.1` — one additional line, matching existing pin style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PyYAML to setup/requirements.txt</name>
  <files>setup/requirements.txt</files>
  <read_first>
    - setup/requirements.txt — read current content before modifying
  </read_first>
  <action>
Append one line to setup/requirements.txt:

```
PyYAML>=6.0.1
```

The existing file contains:
```
flask>=3.1.0
python-dotenv>=1.0.0
```

Result after edit:
```
flask>=3.1.0
python-dotenv>=1.0.0
PyYAML>=6.0.1
```

Use the same `>=` version-pin style as existing entries. No other changes.
  </action>
  <verify>
    <automated>grep "PyYAML>=6.0.1" /Users/home/Projects/thoughts-to-platform-builder/setup/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - setup/requirements.txt contains the exact line `PyYAML>=6.0.1`
    - File still contains `flask>=3.1.0` and `python-dotenv>=1.0.0` (not removed)
    - File has exactly 3 lines (no blank lines added between existing entries)
  </acceptance_criteria>
  <done>setup/requirements.txt declares PyYAML>=6.0.1 alongside Flask and python-dotenv</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirements.txt → pip installer | pip reads and installs declared package versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | setup/requirements.txt | accept | Single-user local tool; no supply-chain risk beyond PyYAML>=6.0.1 pin which matches workflow/requirements.txt |
</threat_model>

<verification>
Run: `grep "PyYAML>=6.0.1" setup/requirements.txt` — must return the line.
Run: `python3 -c "import yaml; print(yaml.__version__)"` in the activated wizard venv after running `pip install -r setup/requirements.txt` — must print a version >= 6.0.1 without error.
</verification>

<success_criteria>
setup/requirements.txt contains PyYAML>=6.0.1. No other files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-form-config-engine/02-01-SUMMARY.md`
</output>
