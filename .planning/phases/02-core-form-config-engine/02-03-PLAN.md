---
phase: 02-core-form-config-engine
plan: 03
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - setup/templates/index.html
autonomous: true
requirements:
  - UX-01
  - UX-03
  - UX-04
  - GMAIL-01
  - GMAIL-02
  - GMAIL-03
  - GMAIL-04
  - LMS-01
  - LMS-02
  - LMS-03
  - LMS-04
must_haves:
  truths:
    - "Progress indicator <nav class='wizard-steps'> is present with all 5 step names"
    - "Active step 'gmail' is rendered with class 'step active'"
    - "Gmail section has: gmail-address, gmail-app-password (with show/hide), gmail-folder, allowed-senders widget"
    - "LM Studio section has: lms-base-url, lms-model, lms-temperature, lms-max-tokens, lms-cli-path"
    - "LM Studio Advanced section uses <details><summary>Advanced</summary> with lms-autostart and lms-request-timeout"
    - "Each required field has aria-describedby referencing both {id}-help and {id}-error"
    - "Each field has a <small id='{id}-help'> with the exact help text from the Copywriting Contract"
    - "Each required field has <span id='{id}-error' class='error' hidden aria-live='polite'>"
    - "External stylesheet link references wizard.css via url_for"
    - "External script tag references wizard.js via url_for"
    - "No inline <style> or inline <script> blocks remain from Phase 1"
    - "Exit Setup button is retained and still calls POST /exit"
  artifacts:
    - path: "setup/templates/index.html"
      provides: "Full wizard form replacing Phase 1 placeholder"
      contains: "wizard-steps"
    - path: "setup/templates/index.html"
      contains: "wizard-form"
  key_links:
    - from: "setup/templates/index.html"
      to: "setup/static/wizard.css"
      via: "url_for('static', filename='wizard.css')"
      pattern: "wizard\\.css"
    - from: "setup/templates/index.html"
      to: "setup/static/wizard.js"
      via: "url_for('static', filename='wizard.js')"
      pattern: "wizard\\.js"
    - from: "setup/templates/index.html"
      to: "setup/server.py"
      via: "{{ active_step }} Jinja2 variable from render_template context"
      pattern: "active_step"
---

<objective>
Replace the Phase 1 placeholder `setup/templates/index.html` with the full wizard form: progress indicator, Gmail section, LM Studio section (including collapsible Advanced), allowed-senders widget, and all field groups conforming to the UI-SPEC.

Purpose: This is the visual shell. JavaScript (Plan 05) wires the behaviour; CSS (Plan 04) styles it. HTML must be complete and structurally correct so Plans 04 and 05 can be written and tested against it in parallel.
Output: setup/templates/index.html — full wizard form, no inline styles, no inline scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-form-config-engine/02-CONTEXT.md
@.planning/phases/02-core-form-config-engine/02-UI-SPEC.md
@.planning/phases/02-core-form-config-engine/02-PATTERNS.md

<interfaces>
<!-- Field IDs and names — these MUST be consistent between index.html, wizard.js, and builder.py -->

Field ID → builder.py key mapping:
- id="gmail-address"       → form_data['gmail_address']
- id="gmail-app-password"  → form_data['gmail_app_password']
- id="gmail-folder"        → form_data['gmail_folder']  (default: INBOX)
- class="sender-input"     → form_data['allowed_senders'] (collected as array by JS)
- id="lms-base-url"        → form_data['lms_base_url']  (default: http://localhost:1234/v1)
- id="lms-model"           → form_data['lms_model']     (default: google/gemma-4-26b-a4b)
- id="lms-temperature"     → form_data['lms_temperature'] (default: 0.4)
- id="lms-max-tokens"      → form_data['lms_max_tokens'] (default: 4096)
- id="lms-cli-path"        → form_data['lms_cli_path']  (default: lms)
- id="lms-autostart"       → form_data['autostart']     (default: true, checkbox)
- id="lms-request-timeout" → form_data['request_timeout_s'] (default: 600)

Fan-out display element IDs (read by wizard.js):
- id="imap-user-preview"   — updated on gmail-address input event
- id="smtp-user-preview"   — updated on gmail-address input event

Canonical field group HTML (from UI-SPEC.md):
```html
<div class="field-group">
  <label for="{id}">{Label}</label>
  <div class="field-row">
    <input id="{id}" name="{name}" type="{type}" value="{default}"
           aria-describedby="{id}-help {id}-error" />
    <!-- show/hide button — only for password/token fields -->
    <button type="button" class="toggle-visibility" data-target="{id}"
            aria-label="Show {label}" aria-pressed="false">Show</button>
  </div>
  <small id="{id}-help" class="help-text">{help copy}</small>
  <span id="{id}-error" class="error" hidden aria-live="polite"></span>
</div>
```

Phase 1 inline script fetch pattern (for Exit Setup — retain verbatim):
```javascript
fetch('/exit', {method: 'POST', headers: {'Content-Type': 'application/json'}})
    .then(function(r) { return r.json(); })
    .then(function(data) { if (data.ok) { ... } })
    .catch(function() { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace setup/templates/index.html with full wizard form</name>
  <files>setup/templates/index.html</files>
  <read_first>
    - setup/templates/index.html — read current Phase 1 content before replacing
    - .planning/phases/02-core-form-config-engine/02-UI-SPEC.md — canonical field structure, help text, copywriting
    - .planning/phases/02-core-form-config-engine/02-PATTERNS.md — HTML patterns to follow
  </read_first>
  <action>
Replace setup/templates/index.html entirely. The new file must:

1. Preserve DOCTYPE, html lang="en", charset, viewport meta, title "Setup Wizard"
2. Replace inline `<style>` block with: `<link rel="stylesheet" href="{{ url_for('static', filename='wizard.css') }}">`
3. Replace inline `<script>` block with: `<script src="{{ url_for('static', filename='wizard.js') }}"></script>` (at end of body)
4. Include the progress indicator nav using Jinja2 active_step variable
5. Include the full wizard form with id="wizard-form" and action="#" (JS handles submit)
6. Include Gmail section, LM Studio section, and Exit Setup button
7. Keep the Exit Setup button functional via inline JS (or move to wizard.js — either is fine, but it must work)

Full structure to implement:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setup Wizard</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='wizard.css') }}">
</head>
<body>

<header>
    <h1>Setup Wizard</h1>
    <nav class="wizard-steps" aria-label="Setup progress">
        {% set steps = [
            ('gmail', 'Gmail Setup'),
            ('lmstudio', 'LM Studio'),
            ('hosting', 'Hosting'),
            ('inboxes', 'Inboxes'),
            ('preview', 'Preview & Write'),
        ] %}
        {% for slug, label in steps %}
        <span class="step {% if slug == active_step %}active{% endif %}" {% if slug == active_step %}aria-current="step"{% endif %}>{{ label }}</span>
        {% endfor %}
    </nav>
</header>

<main>
<form id="wizard-form" novalidate>

    <!-- ── Gmail Setup ───────────────────────────────────────── -->
    <section class="form-section" aria-labelledby="gmail-heading">
        <h2 id="gmail-heading">Gmail Setup</h2>

        <!-- Gmail Address -->
        <div class="field-group">
            <label for="gmail-address">Gmail address</label>
            <div class="field-row">
                <input id="gmail-address" name="gmail_address" type="email"
                       autocomplete="email" aria-describedby="gmail-address-help gmail-address-error" />
            </div>
            <small id="gmail-address-help" class="help-text">
                Your Gmail address. Used for IMAP polling and outgoing mail. —
                <a href="https://support.google.com/accounts/answer/185833" target="_blank" rel="noopener">Gmail app password help</a>
            </small>
            <span id="gmail-address-error" class="error" hidden aria-live="polite"></span>
        </div>

        <!-- Fan-out display -->
        <p class="derived-values">
            This address will be set as your IMAP user (<code id="imap-user-preview"></code>),
            SMTP user (<code id="smtp-user-preview"></code>), and sender address.
        </p>

        <!-- Gmail App Password -->
        <div class="field-group">
            <label for="gmail-app-password">Gmail app password</label>
            <div class="field-row">
                <input id="gmail-app-password" name="gmail_app_password" type="password"
                       autocomplete="off" aria-describedby="gmail-app-password-help gmail-app-password-error" />
                <button type="button" class="toggle-visibility" data-target="gmail-app-password"
                        aria-label="Show Gmail app password" aria-pressed="false">Show</button>
            </div>
            <small id="gmail-app-password-help" class="help-text">
                A 16-character app password from your Google account (not your login password). —
                <a href="https://myaccount.google.com/apppasswords" target="_blank" rel="noopener">Create an app password</a>
            </small>
            <span id="gmail-app-password-error" class="error" hidden aria-live="polite"></span>
        </div>

        <!-- Gmail Folder -->
        <div class="field-group">
            <label for="gmail-folder">Gmail folder</label>
            <div class="field-row">
                <input id="gmail-folder" name="gmail_folder" type="text"
                       value="INBOX" aria-describedby="gmail-folder-help gmail-folder-error" />
            </div>
            <small id="gmail-folder-help" class="help-text">
                The Gmail folder the wizard will poll for new emails. Default: INBOX.
            </small>
            <span id="gmail-folder-error" class="error" hidden aria-live="polite"></span>
        </div>

        <!-- Allowed Senders -->
        <div class="field-group">
            <label>Allowed senders</label>
            <div id="senders-list" aria-describedby="allowed-senders-help allowed-senders-error">
                <div class="sender-row">
                    <input type="email" class="sender-input"
                           placeholder="sender@example.com" autocomplete="off" />
                    <button type="button" class="remove-sender" aria-label="Remove sender" disabled>Remove</button>
                </div>
            </div>
            <button type="button" id="add-sender">Add sender</button>
            <small id="allowed-senders-help" class="help-text">
                Only emails from these addresses will be processed. Add at least one.
            </small>
            <span id="allowed-senders-error" class="error" hidden aria-live="polite"></span>
        </div>

    </section>

    <!-- ── LM Studio Settings ─────────────────────────────────── -->
    <section class="form-section" aria-labelledby="lms-heading">
        <h2 id="lms-heading">LM Studio Settings</h2>

        <!-- Base URL -->
        <div class="field-group">
            <label for="lms-base-url">LM Studio base URL</label>
            <div class="field-row">
                <input id="lms-base-url" name="lms_base_url" type="url"
                       value="http://localhost:1234/v1"
                       aria-describedby="lms-base-url-help lms-base-url-error" />
            </div>
            <small id="lms-base-url-help" class="help-text">
                The local LM Studio API base URL. Leave as default unless you changed the port. —
                <a href="https://lmstudio.ai/docs" target="_blank" rel="noopener">LM Studio docs</a>
            </small>
            <span id="lms-base-url-error" class="error" hidden aria-live="polite"></span>
        </div>

        <!-- Model Tag -->
        <div class="field-group">
            <label for="lms-model">Model tag</label>
            <div class="field-row">
                <input id="lms-model" name="lms_model" type="text"
                       value="google/gemma-4-26b-a4b"
                       aria-describedby="lms-model-help lms-model-error" />
            </div>
            <small id="lms-model-help" class="help-text">
                The model tag loaded in LM Studio. Must match the identifier shown in LM Studio.
            </small>
            <span id="lms-model-error" class="error" hidden aria-live="polite"></span>
        </div>

        <!-- Temperature + Max Tokens (split row) -->
        <div class="field-row-split">
            <div class="field-group">
                <label for="lms-temperature">Temperature</label>
                <div class="field-row">
                    <input id="lms-temperature" name="lms_temperature" type="number"
                           min="0" max="2" step="0.01" value="0.4"
                           aria-describedby="lms-temperature-help lms-temperature-error" />
                </div>
                <small id="lms-temperature-help" class="help-text">
                    Controls response randomness. 0.0 = deterministic, 2.0 = very random. Default: 0.4.
                </small>
                <span id="lms-temperature-error" class="error" hidden aria-live="polite"></span>
            </div>
            <div class="field-group">
                <label for="lms-max-tokens">Max tokens</label>
                <div class="field-row">
                    <input id="lms-max-tokens" name="lms_max_tokens" type="number"
                           min="1" step="1" value="4096"
                           aria-describedby="lms-max-tokens-help lms-max-tokens-error" />
                </div>
                <small id="lms-max-tokens-help" class="help-text">
                    Maximum tokens the model will generate per response. Default: 4096.
                </small>
                <span id="lms-max-tokens-error" class="error" hidden aria-live="polite"></span>
            </div>
        </div>

        <!-- CLI Path -->
        <div class="field-group">
            <label for="lms-cli-path">lms CLI path</label>
            <div class="field-row">
                <input id="lms-cli-path" name="lms_cli_path" type="text"
                       value="lms"
                       aria-describedby="lms-cli-path-help lms-cli-path-error" />
            </div>
            <small id="lms-cli-path-help" class="help-text">
                Path to the lms CLI binary. Use 'lms' if it is on your PATH.
            </small>
            <span id="lms-cli-path-error" class="error" hidden aria-live="polite"></span>
        </div>

        <!-- Advanced (collapsible, per D-02) -->
        <details class="advanced-section">
            <summary>Advanced</summary>

            <div class="field-group">
                <div class="field-row">
                    <input id="lms-autostart" name="autostart" type="checkbox" checked
                           aria-describedby="lms-autostart-help" />
                    <label for="lms-autostart">Autostart LM Studio</label>
                </div>
                <small id="lms-autostart-help" class="help-text">Automatically start LM Studio before running the workflow.</small>
            </div>

            <div class="field-group">
                <label for="lms-request-timeout">Request timeout (seconds)</label>
                <div class="field-row">
                    <input id="lms-request-timeout" name="request_timeout_s" type="number"
                           min="1" step="1" value="600"
                           aria-describedby="lms-request-timeout-help lms-request-timeout-error" />
                </div>
                <small id="lms-request-timeout-help" class="help-text">
                    Seconds to wait for a model response before timing out. Default: 600.
                </small>
                <span id="lms-request-timeout-error" class="error" hidden aria-live="polite"></span>
            </div>
        </details>

    </section>

    <!-- ── Actions ───────────────────────────────────────────── -->
    <div class="form-actions">
        <p id="form-error-summary" class="error" hidden aria-live="polite">Fix the highlighted fields before continuing.</p>
        <button type="submit" class="btn-primary">Save & Continue</button>
    </div>

</form>
</main>

<footer>
    <button id="exit-btn" class="btn-exit" type="button">Exit Setup</button>
</footer>

<script src="{{ url_for('static', filename='wizard.js') }}"></script>
</body>
</html>
```

IMPORTANT constraints:
- NO inline `<style>` blocks — all styles in wizard.css (Plan 04)
- NO inline `<script>` blocks — all JS in wizard.js (Plan 05), except the exit button can remain in wizard.js
- The `<form>` element has `id="wizard-form"` and `novalidate` (JS handles validation)
- All field IDs must match exactly the mapping in the interface block above
- `<details><summary>Advanced</summary>` is the collapsible mechanism for D-02
- `aria-current="step"` on the active progress step satisfies accessibility contract
- Both `id="imap-user-preview"` and `id="smtp-user-preview"` must appear in the derived-values paragraph (wizard.js updates both on gmail-address input events)
  </action>
  <verify>
    <automated>grep -c "field-group" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/index.html</automated>
  </verify>
  <acceptance_criteria>
    - setup/templates/index.html contains `class="wizard-steps"` (progress indicator)
    - setup/templates/index.html contains `active_step` Jinja2 variable reference
    - setup/templates/index.html contains `id="wizard-form"`
    - setup/templates/index.html contains `id="gmail-address"`, `id="gmail-app-password"`, `id="gmail-folder"`
    - setup/templates/index.html contains `id="lms-base-url"`, `id="lms-model"`, `id="lms-temperature"`, `id="lms-max-tokens"`, `id="lms-cli-path"`
    - setup/templates/index.html contains `id="lms-autostart"` and `id="lms-request-timeout"` inside `<details>`
    - setup/templates/index.html contains `class="toggle-visibility"` on the app password show/hide button
    - setup/templates/index.html contains `class="sender-input"` for the allowed senders widget
    - setup/templates/index.html contains `id="add-sender"` button
    - setup/templates/index.html contains `url_for('static', filename='wizard.css')`
    - setup/templates/index.html contains `url_for('static', filename='wizard.js')`
    - setup/templates/index.html does NOT contain `<style>` tag (no inline styles)
    - setup/templates/index.html contains all {id}-error span elements with `hidden` and `aria-live="polite"`
    - setup/templates/index.html contains all {id}-help small elements with the exact help text from the Copywriting Contract
    - setup/templates/index.html contains `id="exit-btn"` button
    - setup/templates/index.html contains both `id="imap-user-preview"` and `id="smtp-user-preview"` in the derived-values paragraph
    - `grep -c "aria-describedby" setup/templates/index.html` returns >= 10 (one per required field including lms-request-timeout and lms-autostart)
  </acceptance_criteria>
  <done>index.html is a complete, valid wizard form with progress indicator, both form sections, all field groups, and no inline styles or scripts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jinja2 template → browser | Server renders active_step variable into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | gmail-app-password field | mitigate | Field type is "password" by default; show/hide toggle toggles type in DOM only — never console.log(). Handled in wizard.js (Plan 05). |
| T-02-03-02 | Tampering | active_step Jinja2 variable | accept | active_step is a server-controlled constant ('gmail') — not user input. No XSS risk. |
| T-02-03-03 | Tampering | novalidate form attribute | mitigate | novalidate is intentional — JS validation handles all rules. Server-side validate() in builder.py is the authoritative gate. Browser constraint validation is disabled to give JS full control. |
</threat_model>

<verification>
Run: `python3 -c "from setup.server import app; client = app.test_client(); r = client.get('/'); print(r.status_code, len(r.data))"` — must return 200 with non-trivial body length (>500 bytes).
Verify: `grep "wizard-steps" setup/templates/index.html` returns a match.
Verify: `grep -c "field-group" setup/templates/index.html` returns >= 9.
Verify: `grep "smtp-user-preview" setup/templates/index.html` returns a match.
</verification>

<success_criteria>
index.html renders through Flask with active_step='gmail', contains all form fields with correct IDs, uses external CSS and JS links, and has no inline styles or scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-form-config-engine/02-03-SUMMARY.md`
</output>
