---
phase: 02-core-form-config-engine
plan: 04
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - setup/static/wizard.css
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
must_haves:
  truths:
    - "Form card is max-width 640px and centred on the page"
    - "Progress indicator steps are displayed in a horizontal row with 24px gap"
    - "Active step renders with accent colour (#2563eb) and bold label"
    - "Inactive steps render with muted text (#6b7280)"
    - "Field error borders are #dc2626; error text is #dc2626"
    - "input[aria-invalid='true'] has border-color #dc2626 (applied by JS)"
    - "Show/Hide toggle button has minimum height 44px"
    - ".field-row is a flexbox row"
    - ".field-row-split is a two-column 50/50 grid for temperature + max_tokens"
    - ".help-text is muted text (#6b7280)"
    - "details > summary cursor is pointer"
    - "Primary CTA button uses accent colour #2563eb"
  artifacts:
    - path: "setup/static/wizard.css"
      provides: "All Phase 2 styles — progress indicator, field groups, error states, typography, spacing"
      min_lines: 120
  key_links:
    - from: "setup/static/wizard.css"
      to: "setup/templates/index.html"
      via: "CSS class selectors target HTML class attributes"
      pattern: "\\.wizard-steps|\\.field-group|\\.step\\.active"
---

<objective>
Create `setup/static/wizard.css` with all Phase 2 styles: design tokens, progress indicator, field groups, error states, show/hide toggle, split row layout, and collapsible Advanced section. No inline styles exist in the HTML — this file is the complete style source.

Purpose: CSS is independent of HTML content and JS behaviour; can be built and refined in parallel with Plans 03 and 05.
Output: setup/static/wizard.css — new file, all design tokens from UI-SPEC.md applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-form-config-engine/02-UI-SPEC.md
@.planning/phases/02-core-form-config-engine/02-PATTERNS.md

<interfaces>
<!-- Design tokens from UI-SPEC.md — exact values, do not re-derive -->

Colors:
- Dominant (background): #ffffff
- Secondary (section bg, progress track): #f3f4f6
- Accent (CTA, active step, focused input outline): #2563eb
- Destructive (error text, error border): #dc2626
- Muted text (help text, inactive steps): #6b7280
- Border default: #d1d5db

Spacing scale:
- xs: 4px
- sm: 8px
- md: 16px
- lg: 24px
- xl: 32px
- 2xl: 48px

Typography:
- Font stack: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
- Body: 16px / 400 / 1.5
- Label: 14px / 600 / 1.4
- Heading (section): 20px / 600 / 1.2
- Display (h1): 24px / 600 / 1.2

Layout:
- Form card max-width: 640px (increased from Phase 1's 600px)
- Page margin-top: 48px (2xl token)
- Show/Hide button minimum height: 44px (accessibility)
- Progress indicator gap between steps: 24px (lg token)

Classes defined in index.html that need CSS rules:
- .wizard-steps, .step, .step.active
- .form-section
- .field-group
- .field-row
- .field-row-split
- .help-text
- .error
- .toggle-visibility
- .derived-values
- .sender-row
- .advanced-section
- .btn-primary
- .btn-exit
- .form-actions
- input[aria-invalid="true"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create setup/static/wizard.css with all Phase 2 styles</name>
  <files>setup/static/wizard.css</files>
  <read_first>
    - .planning/phases/02-core-form-config-engine/02-UI-SPEC.md — spacing scale, color palette, typography, component inventory
    - .planning/phases/02-core-form-config-engine/02-PATTERNS.md — CSS selectors listed, Phase 1 inline style base to extend
  </read_first>
  <action>
Create setup/static/wizard.css as a NEW file. No setup/static/ directory contents currently exist — create the file directly.

The file must cover every CSS class and selector used in index.html (Plan 03). Apply all design tokens exactly as specified in the interface block above.

Required sections and rules:

**1. Base / reset**
```css
*, *::before, *::after { box-sizing: border-box; }

body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    font-size: 16px;
    line-height: 1.5;
    font-weight: 400;
    background-color: #ffffff;
    color: #111827;
    max-width: 640px;
    margin: 48px auto;
    padding: 0 16px;
}
```

**2. Typography**
```css
h1 { font-size: 24px; font-weight: 600; line-height: 1.2; margin-bottom: 16px; }
h2 { font-size: 20px; font-weight: 600; line-height: 1.2; margin-bottom: 16px; }
label { font-size: 14px; font-weight: 600; line-height: 1.4; display: block; margin-bottom: 4px; }
```

**3. Progress indicator (.wizard-steps, .step, .step.active)**
```css
.wizard-steps {
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
    margin-bottom: 32px;
    padding: 0;
    list-style: none;
}
.step {
    font-size: 14px;
    color: #6b7280;
    position: relative;
    padding-left: 16px;
}
.step::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: #d1d5db;
}
.step.active {
    font-weight: 600;
    color: #2563eb;
}
.step.active::before {
    background-color: #2563eb;
}
```

**4. Form sections**
```css
.form-section {
    background-color: #f3f4f6;
    border: 1px solid #d1d5db;
    border-radius: 8px;
    padding: 24px;
    margin-bottom: 24px;
}
```

**5. Field groups (.field-group, .field-row, .field-row-split)**
```css
.field-group {
    margin-bottom: 16px;
}
.field-row {
    display: flex;
    align-items: stretch;
    gap: 8px;
}
.field-row-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
}
```

**6. Inputs**
```css
input[type="text"],
input[type="email"],
input[type="password"],
input[type="url"],
input[type="number"] {
    width: 100%;
    padding: 8px 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    font-size: 16px;
    font-family: inherit;
    background-color: #ffffff;
    color: #111827;
}
input[type="text"]:focus,
input[type="email"]:focus,
input[type="password"]:focus,
input[type="url"]:focus,
input[type="number"]:focus {
    outline: 2px solid #2563eb;
    outline-offset: 2px;
    border-color: #2563eb;
}
input[aria-invalid="true"] {
    border-color: #dc2626;
}
input[aria-invalid="true"]:focus {
    outline-color: #dc2626;
}
```

**7. Help text and error states**
```css
.help-text {
    display: block;
    font-size: 14px;
    color: #6b7280;
    margin-top: 4px;
}
.error {
    display: block;
    font-size: 14px;
    color: #dc2626;
    margin-top: 4px;
}
.error[hidden] { display: none; }
```

**8. Show/Hide toggle**
```css
.toggle-visibility {
    min-height: 44px;
    padding: 0 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    background-color: #ffffff;
    color: #374151;
    font-size: 14px;
    cursor: pointer;
    white-space: nowrap;
    flex-shrink: 0;
}
.toggle-visibility:hover {
    background-color: #f3f4f6;
}
```

**9. Allowed senders widget**
```css
#senders-list { margin-bottom: 8px; }
.sender-row {
    display: flex;
    gap: 8px;
    margin-bottom: 8px;
    align-items: center;
}
.sender-row input { flex: 1; }
.remove-sender {
    min-height: 44px;
    padding: 0 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    background-color: #ffffff;
    color: #dc2626;
    font-size: 14px;
    cursor: pointer;
    white-space: nowrap;
    flex-shrink: 0;
}
.remove-sender:disabled { color: #d1d5db; cursor: not-allowed; }
#add-sender {
    font-size: 14px;
    color: #2563eb;
    background: none;
    border: none;
    padding: 4px 0;
    cursor: pointer;
    text-decoration: underline;
}
```

**10. Advanced collapsible section**
```css
.advanced-section {
    border: 1px solid #d1d5db;
    border-radius: 6px;
    padding: 0;
    margin-top: 16px;
}
.advanced-section > summary {
    font-size: 14px;
    font-weight: 600;
    color: #374151;
    cursor: pointer;
    padding: 12px 16px;
    list-style: none;
}
.advanced-section > summary::-webkit-details-marker { display: none; }
.advanced-section[open] > summary { border-bottom: 1px solid #d1d5db; }
.advanced-section > div,
.advanced-section > .field-group {
    padding: 16px;
}
```

**11. Derived values callout**
```css
.derived-values {
    font-size: 14px;
    color: #6b7280;
    background-color: #ffffff;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    padding: 8px 12px;
    margin-bottom: 16px;
}
.derived-values code {
    font-family: monospace;
    color: #2563eb;
}
```

**12. Buttons (primary CTA, exit)**
```css
.btn-primary {
    display: inline-block;
    padding: 10px 24px;
    background-color: #2563eb;
    color: #ffffff;
    border: none;
    border-radius: 6px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    min-height: 44px;
}
.btn-primary:hover { background-color: #1d4ed8; }
.btn-primary:disabled { background-color: #93c5fd; cursor: not-allowed; }
.btn-exit {
    font-size: 14px;
    color: #6b7280;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px 0;
    text-decoration: underline;
}
.btn-exit:hover { color: #dc2626; }
```

**13. Form actions area and footer**
```css
.form-actions {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 8px;
    margin-top: 8px;
}
footer {
    margin-top: 32px;
    padding-top: 16px;
    border-top: 1px solid #d1d5db;
}
#form-error-summary {
    font-size: 14px;
}
```

**14. Checkbox layout (autostart)**
```css
input[type="checkbox"] {
    width: auto;
    margin-right: 8px;
    cursor: pointer;
}
.field-row:has(input[type="checkbox"]) {
    align-items: center;
}
.field-row:has(input[type="checkbox"]) label {
    margin-bottom: 0;
    font-weight: 400;
}
```

Write all sections in order into a single wizard.css file. No external imports, no CSS variables (use literal hex values as specified — no build step).
  </action>
  <verify>
    <automated>grep -c "#2563eb" /Users/home/Projects/thoughts-to-platform-builder/setup/static/wizard.css</automated>
  </verify>
  <acceptance_criteria>
    - setup/static/wizard.css exists (file created)
    - File contains `max-width: 640px` (form card width per UI-SPEC)
    - File contains `margin: 48px auto` (2xl spacing token)
    - File contains `.wizard-steps` selector
    - File contains `.step.active` selector with `color: #2563eb`
    - File contains `input[aria-invalid="true"]` selector with `border-color: #dc2626`
    - File contains `.toggle-visibility` selector with `min-height: 44px`
    - File contains `.field-row-split` selector with `grid-template-columns: 1fr 1fr`
    - File contains `.help-text` selector with `color: #6b7280`
    - File contains `.error` selector with `color: #dc2626`
    - File contains `.btn-primary` selector with `background-color: #2563eb`
    - File contains `details` or `.advanced-section` selector with `cursor: pointer` on summary
    - File does NOT contain `@import` (no external imports)
    - File does NOT contain `var(--` (no CSS custom properties — no build step)
    - `wc -l setup/static/wizard.css` returns >= 120
  </acceptance_criteria>
  <done>wizard.css exists with all design tokens applied, all required selectors present, and no inline styles needed in HTML</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → static file server | wizard.css served as a static asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | CSS file contents | accept | CSS is a public static asset by design; contains no sensitive data |
| T-02-04-02 | Tampering | CSS selectors hiding error spans | mitigate | `.error[hidden] { display: none; }` must be explicit — if this rule is missing, the `hidden` HTML attribute is overridden by `.error { display: block; }`. Both rules must coexist. |
</threat_model>

<verification>
Run: `grep "min-height: 44px" setup/static/wizard.css` — must match at least once (toggle button accessibility).
Run: `grep "input\[aria-invalid" setup/static/wizard.css` — must match (error border state).
Run: `grep ".step.active" setup/static/wizard.css` — must match (active progress step).
</verification>

<success_criteria>
wizard.css exists with all UI-SPEC design tokens applied. All CSS selectors targeting classes/states used in index.html are defined. File is at least 120 lines.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-form-config-engine/02-04-SUMMARY.md`
</output>
