---
phase: 01-server-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - setup/requirements.txt
  - scripts/setup.sh
autonomous: true
requirements:
  - SRV-01

must_haves:
  truths:
    - "Running ./scripts/setup.sh activates the venv and launches the wizard"
    - "setup/requirements.txt lists flask>=3.1.0 and python-dotenv>=1.0.0"
    - "setup.sh is executable and uses the project venv at .venv/"
    - "After setup.sh exits, the terminal prints the next-step command"
  artifacts:
    - path: "setup/requirements.txt"
      provides: "Wizard-specific pip dependencies (flask, python-dotenv)"
    - path: "scripts/setup.sh"
      provides: "Shell entry point: venv activation + python -m setup.server"
  key_links:
    - from: "scripts/setup.sh"
      to: "python -m setup.server"
      via: "exec after venv activation"
    - from: "scripts/setup.sh"
      to: ".venv/bin/activate"
      via: "source .venv/bin/activate"
---

<objective>
Create the shell entry point (scripts/setup.sh) and the wizard-specific requirements file (setup/requirements.txt).

Purpose: SRV-01 requires the user to run ./scripts/setup.sh and have the wizard launch automatically. The shell wrapper handles venv activation and pip-installs the wizard dependencies before delegating to python -m setup.server. This plan is independent of Plan 01 (no shared files) so it runs in Wave 1 in parallel.

Output: setup/requirements.txt, scripts/setup.sh
</objective>

<execution_context>
@/Users/home/.claude/get-shit-done/workflows/execute-plan.md
@/Users/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/home/Projects/thoughts-to-platform-builder/.planning/ROADMAP.md
@/Users/home/Projects/thoughts-to-platform-builder/.planning/REQUIREMENTS.md

<interfaces>
<!-- Existing scripts for style reference -->
From scripts/run-workflow.sh pattern (existing):
  - Uses #!/usr/bin/env bash with set -euo pipefail
  - Activates .venv at project root: source "$(dirname "$0")/../.venv/bin/activate"
  - Prints next-step instructions after the Python process exits

New wizard requirements (separate from workflow/requirements.txt):
  flask>=3.1.0          # HTTP server + Jinja2
  python-dotenv>=1.0.0  # .env writing
  PyYAML is intentionally excluded from setup/requirements.txt — it is already
  in workflow/requirements.txt and will be available after the venv is populated.

Entry point:
  python -m setup.server    (setup/ package is at repo root; venv python must be used)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create setup/requirements.txt with wizard dependencies</name>
  <files>setup/requirements.txt</files>
  <action>
    Create setup/requirements.txt with exactly these contents (no extras, no pinned patch versions):

      flask>=3.1.0
      python-dotenv>=1.0.0

    PyYAML is intentionally absent — it ships via workflow/requirements.txt which the venv already has.
    Do NOT add pytest, coverage, or any dev dependency to this file.
    Do NOT add a comment header — the file should contain only the two package specifiers.
  </action>
  <verify>
    <automated>grep -c "flask\|python-dotenv" /Users/home/Projects/thoughts-to-platform-builder/setup/requirements.txt</automated>
  </verify>
  <done>setup/requirements.txt exists with exactly two lines: flask>=3.1.0 and python-dotenv>=1.0.0. grep -c returns 2.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/setup.sh entry point</name>
  <files>scripts/setup.sh</files>
  <action>
    Create scripts/setup.sh. It must:

    1. Start with #!/usr/bin/env bash
    2. Set strict mode: set -euo pipefail
    3. Resolve REPO_ROOT to the directory containing the scripts/ directory:
         REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
    4. Check that .venv/bin/activate exists; if not, print a clear error and exit 1:
         if [[ ! -f "$REPO_ROOT/.venv/bin/activate" ]]; then
           echo "ERROR: .venv not found. Run: python3 -m venv .venv && .venv/bin/pip install -r workflow/requirements.txt" >&2
           exit 1
         fi
    5. Activate the venv:
         source "$REPO_ROOT/.venv/bin/activate"
    6. Install/upgrade wizard-specific requirements (silent on already-installed):
         pip install -q -r "$REPO_ROOT/setup/requirements.txt"
    7. Change to the repo root so that python -m setup.server resolves REPO_ROOT correctly:
         cd "$REPO_ROOT"
    8. Run the wizard:
         python -m setup.server
    9. After the wizard exits, print next-step instructions:
         echo ""
         echo "Setup complete."
         echo "To start the workflow:"
         echo "  ./scripts/run-workflow.sh"

    After writing the file, make it executable:
         chmod +x /Users/home/Projects/thoughts-to-platform-builder/scripts/setup.sh

    Verify the shebang and strict mode are present before marking done.
  </action>
  <verify>
    <automated>bash -n /Users/home/Projects/thoughts-to-platform-builder/scripts/setup.sh &amp;&amp; echo "syntax OK" &amp;&amp; head -3 /Users/home/Projects/thoughts-to-platform-builder/scripts/setup.sh</automated>
  </verify>
  <done>bash -n reports no syntax errors. File is executable (ls -l shows -rwxr-xr-x or similar). Head shows #!/usr/bin/env bash and set -euo pipefail on lines 1-2. File contains python -m setup.server and the post-exit echo block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell → Python | setup.sh activates the project venv before delegating; no user-controlled PATH injection |
| pip install | Requirements file is repo-internal; no dynamic version resolution from untrusted sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | pip install in setup.sh | accept | Requirements file is version-pinned with >=; pip resolves from PyPI over HTTPS; no user-controlled input reaches the install command |
| T-02-02 | Elevation of Privilege | PATH manipulation before venv activation | mitigate | REPO_ROOT computed from $0 (script location), not $PWD or env; venv activation overwrites PATH cleanly |
| T-02-03 | Denial of Service | Missing .venv | mitigate | Explicit existence check with clear error message and exit 1 before any action |
</threat_model>

<verification>
From the project root:

1. File exists and is executable:
   ls -la scripts/setup.sh
   (expect -rwxr-xr-x)

2. Bash syntax check:
   bash -n scripts/setup.sh && echo OK

3. Requirements file correct:
   cat setup/requirements.txt
   (expect exactly: flask>=3.1.0 and python-dotenv>=1.0.0)

4. Script contains required strings:
   grep -n "set -euo pipefail\|python -m setup.server\|run-workflow.sh" scripts/setup.sh
   (all three must appear)

5. Venv guard present:
   grep -n ".venv/bin/activate" scripts/setup.sh
   (must appear in the existence check and the source line)
</verification>

<success_criteria>
- setup/requirements.txt contains flask>=3.1.0 and python-dotenv>=1.0.0 (exactly two lines, no extras)
- scripts/setup.sh is executable, passes bash -n, has set -euo pipefail, venv guard, pip install -q, cd REPO_ROOT, python -m setup.server, and post-exit next-step echo
- No workflow/requirements.txt was modified
</success_criteria>

<output>
After completion, create /Users/home/Projects/thoughts-to-platform-builder/.planning/phases/01-server-foundation/01-02-SUMMARY.md using the summary template.
</output>
