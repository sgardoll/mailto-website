---
phase: 03
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - setup/templates/index.html
  - setup/templates/lmstudio.html
autonomous: true
requirements:
  - HOST-01

must_haves:
  truths:
    - "GET / renders gmail step only (no LM Studio section in index.html)"
    - "GET /step/lmstudio renders lmstudio.html with active_step='lmstudio'"
    - "lmstudio.html contains all LM Studio fields identical to the removed section in index.html"
    - "Progress indicator in lmstudio.html highlights the lmstudio step"
  artifacts:
    - path: "setup/templates/lmstudio.html"
      provides: "LM Studio wizard step template"
      contains: "lmstudio"
    - path: "setup/templates/index.html"
      provides: "Gmail-only wizard step template (LM Studio section removed)"
  key_links:
    - from: "setup/templates/index.html"
      to: "setup/server.py"
      via: "GET / route passes active_step='gmail'"
      pattern: "active_step='gmail'"
    - from: "setup/templates/lmstudio.html"
      to: "setup/server.py"
      via: "GET /step/lmstudio route passes active_step='lmstudio'"
      pattern: "active_step='lmstudio'"
---

<objective>
Split the combined Gmail+LM Studio index.html into two separate templates: index.html (Gmail only) and a new lmstudio.html (LM Studio step). Per decision D-01, each wizard step has its own Jinja2 template.

Purpose: The wizard is multi-page. Without separate templates, the hosting and inboxes steps cannot be added as standalone pages. This establishes the template-per-step pattern.
Output: index.html stripped to Gmail section only; lmstudio.html with full LM Studio section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hosting-provider-inbox-manager/03-CONTEXT.md
@.planning/phases/03-hosting-provider-inbox-manager/03-UI-SPEC.md

<interfaces>
<!-- Pattern from index.html to replicate in lmstudio.html -->
<!-- Header/nav block (identical in every template): -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setup Wizard</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='wizard.css') }}">
</head>
<body>
<header>
    <h1>Setup Wizard</h1>
    <nav class="wizard-steps" aria-label="Setup progress">
        {% set steps = [
            ('gmail', 'Gmail Setup'),
            ('lmstudio', 'LM Studio'),
            ('hosting', 'Hosting'),
            ('inboxes', 'Inboxes'),
            ('preview', 'Preview & Write'),
        ] %}
        {% for slug, label in steps %}
        <span class="step {% if slug == active_step %}active{% endif %}" {% if slug == active_step %}aria-current="step"{% endif %}>{{ label }}</span>
        {% endfor %}
    </nav>
</header>

<!-- Form actions block (identical in every template): -->
<div class="form-actions">
    <p id="form-error-summary" class="error" hidden aria-live="polite">Fix the highlighted fields before continuing.</p>
    <button type="submit" class="btn-primary">Save &amp; Continue</button>
</div>

<!-- Footer (identical in every template): -->
<footer>
    <button id="exit-btn" class="btn-exit" type="button">Exit Setup</button>
</footer>
<script src="{{ url_for('static', filename='wizard.js') }}"></script>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract LM Studio section into lmstudio.html</name>
  <read_first>
    - setup/templates/index.html (full file — copy header, footer, form-actions pattern; extract the LM Studio section verbatim)
  </read_first>
  <action>
Create setup/templates/lmstudio.html as a complete HTML page. It must:

1. Use the identical header/nav block from index.html with active_step='lmstudio' (passed from server)
2. Contain a `<form id="wizard-form" novalidate>` wrapping one `<section class="form-section" aria-labelledby="lms-heading">`
3. Inside the section: the complete LM Studio section content copied verbatim from index.html lines 103–203 (the `<section class="form-section" aria-labelledby="lms-heading">` ... `</section>` block) — DO NOT alter any field IDs, names, default values, aria attributes, or help text
4. The form-actions block (form-error-summary + Save & Continue button) identical to index.html
5. The footer and script tags identical to index.html

The complete LM Studio section to copy (from index.html) contains:
- `<h2 id="lms-heading">LM Studio Settings</h2>`
- Base URL field: id="lms-base-url", name="lms_base_url", value="http://localhost:1234/v1"
- Model tag field: id="lms-model", name="lms_model", value="google/gemma-4-26b-a4b"
- Split row with temperature (id="lms-temperature") and max tokens (id="lms-max-tokens")
- CLI path field: id="lms-cli-path", name="lms_cli_path", value="lms"
- Advanced `<details>` section with autostart checkbox and request_timeout_s
  </action>
  <verify>
    <automated>grep -n "lms-base-url\|lms-model\|lms-temperature\|lms-max-tokens\|lms-cli-path\|lms-autostart" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/lmstudio.html | wc -l</automated>
  </verify>
  <done>
    - setup/templates/lmstudio.html exists
    - Contains all 6 LM Studio field IDs (lms-base-url, lms-model, lms-temperature, lms-max-tokens, lms-cli-path, lms-autostart)
    - active_step variable used in nav (not hardcoded)
    - Has form-actions, footer, and script tag
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip LM Studio section from index.html (Gmail step only)</name>
  <read_first>
    - setup/templates/index.html (full file — identify exact lines to remove)
    - setup/templates/lmstudio.html (confirm Task 1 complete before editing index.html)
  </read_first>
  <action>
Edit setup/templates/index.html to remove the LM Studio section. Specifically:

Remove the entire block from:
  `<!-- ── LM Studio Settings ─────────────────────────────────── -->`
through (and including):
  `</section>` (the closing tag of `<section class="form-section" aria-labelledby="lms-heading">`)

The resulting index.html must still have:
- The complete header/nav block (unchanged)
- The Gmail Setup section (unchanged, lines 31–100 approximately)
- The form-actions block (unchanged)
- The footer and script tag (unchanged)

Do NOT modify any Gmail fields, IDs, names, aria attributes, help text, or default values.
Do NOT modify the progress indicator steps list — all 5 steps must remain.

After editing, verify the file no longer contains 'lms-base-url' but still contains 'gmail-address'.
  </action>
  <acceptance_criteria>
    - grep -c "lms-base-url" setup/templates/index.html returns 0
    - grep -c "gmail-address" setup/templates/index.html returns at least 1
    - grep -c "lmstudio" setup/templates/index.html returns at least 1 (the step slug in nav)
    - File parses as valid HTML (no unclosed tags from the removal)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "lms-base-url" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/index.html && grep -c "gmail-address" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/index.html</automated>
  </verify>
  <done>
    - index.html has 0 occurrences of "lms-base-url"
    - index.html still has the gmail-address field
    - index.html still has the 5-step progress indicator nav
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template rendering | Server renders templates with active_step variable from route handler; no user input in templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Jinja2 template variable active_step | accept | Variable is set by server route, not from user input; no injection risk |
</threat_model>

<verification>
# Verify lmstudio.html exists and has all LM Studio fields
grep -c "lms-base-url\|lms-model\|lms-temperature\|lms-max-tokens\|lms-cli-path" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/lmstudio.html

# Verify index.html no longer has LM Studio section
grep -c "lms-base-url" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/index.html
# Expected: 0

# Verify index.html still has Gmail fields
grep -c "gmail-address" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/index.html
# Expected: >= 1
</verification>

<success_criteria>
- setup/templates/lmstudio.html exists with complete LM Studio form section
- setup/templates/index.html contains only Gmail Setup section (no LM Studio fields)
- Both templates have identical header/nav structure with the 5-step progress indicator
- No field IDs or names were changed during extraction
</success_criteria>

<output>
After completion, create `.planning/phases/03-hosting-provider-inbox-manager/03-02-SUMMARY.md`
</output>
