---
phase: 03
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - setup/templates/hosting.html
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
  - HOST-04
  - HOST-05
  - HOST-06

must_haves:
  truths:
    - "hosting.html renders with active_step='hosting' in the progress indicator"
    - "Provider <select> has all 5 options in correct order with correct values"
    - "SiteGround field group is visible by default; all others have hidden attribute"
    - "All 5 provider field groups are present with correct field IDs and names"
    - "SSH provider fields include show/hide toggles on password inputs"
    - "Each field has a label, help text, and error span with correct IDs"
  artifacts:
    - path: "setup/templates/hosting.html"
      provides: "Hosting wizard step template"
      contains: "hosting-provider"
  key_links:
    - from: "setup/templates/hosting.html"
      to: "setup/static/wizard.js"
      via: "JS reads #hosting-provider select and .provider-fields divs"
      pattern: "id=\"hosting-provider\"\|class=\"provider-fields\""
    - from: "setup/templates/hosting.html"
      to: "setup/server.py"
      via: "GET /step/hosting renders this template with active_step='hosting'"
      pattern: "active_step"
---

<objective>
Create setup/templates/hosting.html — the hosting provider wizard step. The page has a provider dropdown that conditionally reveals one of five credential field groups. Only the SiteGround group is visible on load; all others have the `hidden` attribute.

Purpose: Users must be able to select their hosting provider and enter the relevant credentials. JS (Plan G) will handle show/hide on dropdown change; this plan creates the static HTML structure that JS targets.
Output: hosting.html with all 5 provider field groups, correct field IDs, help text, and error spans per the UI-SPEC copywriting contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hosting-provider-inbox-manager/03-CONTEXT.md
@.planning/phases/03-hosting-provider-inbox-manager/03-UI-SPEC.md

<interfaces>
<!-- Field IDs and names (from UI-SPEC): -->
<!-- Provider select: id="hosting-provider", name="hosting_provider" -->
<!-- Provider field groups: class="provider-fields", data-provider="{value}", hidden (except siteground) -->

<!-- SiteGround group (data-provider="siteground", NO hidden attribute): -->
<!--   sg-host (text), sg-port (number, value=18765), sg-username (text) -->
<!--   sg-ssh_key_path (text), sg-password (password + toggle) -->
<!--   sg-remote_base_path (text) -->

<!-- SSH/SFTP group (data-provider="ssh_sftp", hidden): -->
<!--   ssh-host (text), ssh-port (number, value=22), ssh-username (text) -->
<!--   ssh-ssh_key_path (text), ssh-password (password + toggle) -->
<!--   ssh-remote_base_path (text) -->

<!-- Netlify group (data-provider="netlify", hidden): -->
<!--   netlify-api-token (password + toggle), netlify-site-id (text) -->

<!-- Vercel group (data-provider="vercel", hidden): -->
<!--   vercel-api-token (password + toggle), vercel-project-id (text) -->

<!-- GitHub Pages group (data-provider="github_pages", hidden): -->
<!--   gh-pages-branch (text, value="gh-pages") -->

<!-- Error span IDs follow pattern: {field-id}-error -->
<!-- Help text span IDs follow pattern: {field-id}-help -->
<!-- Show/hide button: class="toggle-visibility", data-target="{field-id}" -->

<!-- Copywriting (from UI-SPEC): -->
<!-- help text hosting-provider: "Select the platform where your generated sites will be published." -->
<!-- help sg-host: "Hostname or IP address of your SSH server." -->
<!-- help sg-port: "SSH port for your SiteGround account. Default: 18765." -->
<!-- help ssh-port: "SSH/SFTP port. Default: 22." -->
<!-- help sg-username/ssh-username: "Your SSH username." -->
<!-- help sg-ssh_key_path/ssh-ssh_key_path: "Absolute path to your SSH private key file (e.g. /home/user/.ssh/id_rsa). Leave blank if using a password." -->
<!-- help sg-password/ssh-password: "SSH password. Leave blank if using a key file. — Only one of key path or password is required." -->
<!-- help sg-remote_base_path/ssh-remote_base_path: "Absolute path on the remote server where site files will be uploaded (e.g. /home/user/public_html)." -->
<!-- help netlify-api-token: 'Your Netlify personal access token. — <a href="https://app.netlify.com/user/applications" target="_blank" rel="noopener">Create a token</a>' -->
<!-- help netlify-site-id: "The Netlify site ID (found in Site Settings > General)." -->
<!-- help vercel-api-token: 'Your Vercel API token. — <a href="https://vercel.com/account/tokens" target="_blank" rel="noopener">Create a token</a>' -->
<!-- help vercel-project-id: "Your Vercel project name or ID (found in Project Settings)." -->
<!-- help gh-pages-branch: "The Git branch GitHub Pages publishes from. Default: gh-pages." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hosting.html with provider dropdown and all 5 field groups</name>
  <read_first>
    - setup/templates/index.html (copy header/nav/footer/script pattern exactly; note field-group structure)
    - .planning/phases/03-hosting-provider-inbox-manager/03-UI-SPEC.md (field IDs, names, help text, validation table, accessibility contract)
    - .planning/phases/03-hosting-provider-inbox-manager/03-CONTEXT.md (D-04, D-05, D-06 for field group structure)
  </read_first>
  <action>
Create setup/templates/hosting.html. Structure:

1. HEADER (identical to index.html — copy verbatim):
   - DOCTYPE, html, head with wizard.css link
   - `<body>`, `<header>` with `<h1>Setup Wizard</h1>`
   - Nav with all 5 steps; active_step variable used for .active class and aria-current

2. MAIN:
   ```html
   <main>
   <form id="wizard-form" novalidate>
   <section class="form-section" aria-labelledby="hosting-heading">
     <h2 id="hosting-heading">Hosting Provider</h2>

     <!-- Provider dropdown -->
     <div class="field-group">
       <label for="hosting-provider">Hosting provider</label>
       <div class="field-row">
         <select id="hosting-provider" name="hosting_provider" aria-describedby="hosting-provider-help">
           <option value="siteground" selected>SiteGround</option>
           <option value="ssh_sftp">Generic SSH/SFTP</option>
           <option value="netlify">Netlify</option>
           <option value="vercel">Vercel</option>
           <option value="github_pages">GitHub Pages</option>
         </select>
       </div>
       <small id="hosting-provider-help" class="help-text">Select the platform where your generated sites will be published.</small>
     </div>

     <!-- SiteGround fields (visible by default — NO hidden attribute) -->
     <div class="provider-fields" data-provider="siteground">
       <!-- 6 field-groups: sg-host, sg-port, sg-username, sg-ssh_key_path, sg-password (with toggle), sg-remote_base_path -->
     </div>

     <!-- Generic SSH/SFTP fields (hidden) -->
     <div class="provider-fields" data-provider="ssh_sftp" hidden aria-hidden="true">
       <!-- 6 field-groups: ssh-host, ssh-port, ssh-username, ssh-ssh_key_path, ssh-password (with toggle), ssh-remote_base_path -->
     </div>

     <!-- Netlify fields (hidden) -->
     <div class="provider-fields" data-provider="netlify" hidden aria-hidden="true">
       <!-- 2 field-groups: netlify-api-token (with toggle), netlify-site-id -->
     </div>

     <!-- Vercel fields (hidden) -->
     <div class="provider-fields" data-provider="vercel" hidden aria-hidden="true">
       <!-- 2 field-groups: vercel-api-token (with toggle), vercel-project-id -->
     </div>

     <!-- GitHub Pages fields (hidden) -->
     <div class="provider-fields" data-provider="github_pages" hidden aria-hidden="true">
       <!-- 1 field-group: gh-pages-branch -->
     </div>

   </section>
   ```

3. Each field-group within a provider follows this exact pattern (use id="sg-host" etc.):
   ```html
   <div class="field-group">
     <label for="{id}">{Label}</label>
     <div class="field-row">
       <input id="{id}" name="{id converted to underscores if needed}" type="{type}"
              aria-describedby="{id}-help {id}-error" {value=""} {min/max/step for number} />
       <!-- For password fields only: -->
       <button type="button" class="toggle-visibility" data-target="{id}"
               aria-label="Show {label}" aria-pressed="false">Show</button>
     </div>
     <small id="{id}-help" class="help-text">{help text per UI-SPEC}</small>
     <span id="{id}-error" class="error" hidden aria-live="polite"></span>
   </div>
   ```

4. Field specifications:
   - sg-host: type="text", no default
   - sg-port: type="number" min="1" max="65535" step="1" value="18765"
   - sg-username: type="text", no default
   - sg-ssh_key_path: type="text", no default (use id="sg-ssh_key_path", name="sg-ssh_key_path")
   - sg-password: type="password" with Show/Hide toggle (id="sg-password", name="sg-password")
   - sg-remote_base_path: type="text", no default (use id="sg-remote_base_path")
   - ssh-* fields: same structure as sg-* but with prefix "ssh-" and ssh-port value="22"
   - netlify-api-token: type="password" with toggle (id="netlify-api-token", name="netlify_api_token")
   - netlify-site-id: type="text" (id="netlify-site-id", name="netlify_site_id")
   - vercel-api-token: type="password" with toggle (id="vercel-api-token", name="vercel_api_token")
   - vercel-project-id: type="text" (id="vercel-project-id", name="vercel_project_id")
   - gh-pages-branch: type="text" value="gh-pages" (id="gh-pages-branch", name="gh_pages_branch")

   IMPORTANT for name attributes: use underscores not hyphens for the name attribute where field IDs use hyphens, matching the builder function parameter names:
   - id="sg-host" -> name="sg-host" (keep hyphens — builder uses form_data.get('sg-host'))
   - id="sg-ssh_key_path" -> name="sg-ssh_key_path"
   - id="netlify-api-token" -> name="netlify_api_token" (builder uses 'netlify_api_token')
   - id="netlify-site-id" -> name="netlify_site_id"
   - id="vercel-api-token" -> name="vercel_api_token"
   - id="vercel-project-id" -> name="vercel_project_id"
   - id="gh-pages-branch" -> name="gh_pages_branch"
   All sg-* and ssh-* field names keep their hyphens to match builder.py validate_hosting() field keys.

5. FORM ACTIONS:
   ```html
   <div class="form-actions">
     <p id="form-error-summary" class="error" hidden aria-live="polite">Fix the highlighted fields before continuing.</p>
     <button type="submit" class="btn-primary">Save &amp; Continue</button>
   </div>
   </form>
   </main>
   ```

6. FOOTER (identical to index.html):
   ```html
   <footer>
     <button id="exit-btn" class="btn-exit" type="button">Exit Setup</button>
   </footer>
   <script src="{{ url_for('static', filename='wizard.js') }}"></script>
   ```
  </action>
  <acceptance_criteria>
    - grep -c "provider-fields" hosting.html returns 5 (one per provider)
    - grep -c 'data-provider=' hosting.html returns 5
    - grep -c 'hidden' hosting.html returns 4 (ssh_sftp, netlify, vercel, github_pages — NOT siteground)
    - grep -c 'toggle-visibility' hosting.html returns at least 4 (sg-password, ssh-password, netlify-api-token, vercel-api-token)
    - grep -c 'sg-host\|sg-port\|sg-username\|sg-ssh_key_path\|sg-password\|sg-remote_base_path' hosting.html returns at least 6
    - grep -c 'netlify-api-token\|netlify-site-id' hosting.html returns at least 2
    - grep -c 'gh-pages-branch' hosting.html returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c "provider-fields" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/hosting.html && grep -c "hidden" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/hosting.html</automated>
  </verify>
  <done>
    - setup/templates/hosting.html exists
    - 5 .provider-fields groups present (siteground without hidden, 4 others with hidden aria-hidden="true")
    - All field IDs from UI-SPEC present
    - Show/Hide toggles on password fields
    - Help text per UI-SPEC copywriting contract
    - Error spans for all fields
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template to browser | HTML rendered server-side; no user data in template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | Password fields (sg-password, netlify-api-token, etc.) | mitigate | All credential inputs use type="password"; Show/Hide toggle follows existing pattern |
</threat_model>

<verification>
# All 5 provider groups exist
grep -c "provider-fields" /Users/home/Projects/thoughts-to-platform-builder/setup/templates/hosting.html
# Expected: 5

# Exactly 4 hidden attributes (siteground is visible by default)
grep -o 'hidden' /Users/home/Projects/thoughts-to-platform-builder/setup/templates/hosting.html | wc -l
# Expected: >= 4 (4 hidden on groups, more on error spans)

# SiteGround group NOT hidden
grep -A1 'data-provider="siteground"' /Users/home/Projects/thoughts-to-platform-builder/setup/templates/hosting.html
# Must NOT contain "hidden" on that line
</verification>

<success_criteria>
- hosting.html exists in setup/templates/
- 5 .provider-fields divs, each with correct data-provider value
- SiteGround group has no hidden attribute; all 4 others have hidden and aria-hidden="true"
- All field IDs match what builder.py validate_hosting() expects
- Help text matches UI-SPEC copywriting contract verbatim
- Error spans follow {field-id}-error pattern
</success_criteria>

<output>
After completion, create `.planning/phases/03-hosting-provider-inbox-manager/03-03-SUMMARY.md`
</output>
