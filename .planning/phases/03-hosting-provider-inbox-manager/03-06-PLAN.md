---
phase: 03
plan: 06
type: execute
wave: 2
depends_on:
  - 03-03-PLAN.md
  - 03-04-PLAN.md
files_modified:
  - setup/static/wizard.css
autonomous: true
requirements:
  - HOST-01
  - HOST-06
  - INBOX-01
  - INBOX-02
  - INBOX-03

must_haves:
  truths:
    - "select element styled to match input appearance (same border, padding, font-size)"
    - ".provider-fields[hidden] { display: none } rule is present"
    - ".field-row-thirds creates a 3-column grid"
    - ".inbox-row has background #f3f4f6 border and padding"
    - ".remove-inbox matches .remove-sender styling"
    - "#add-inbox matches #add-sender styling"
    - "No existing CSS rules were modified"
  artifacts:
    - path: "setup/static/wizard.css"
      provides: "Phase 3 CSS additions appended"
      contains: ".field-row-thirds"
  key_links:
    - from: "setup/static/wizard.css .provider-fields[hidden]"
      to: "setup/templates/hosting.html .provider-fields[hidden]"
      via: "CSS hides groups with hidden attribute; JS removes/adds the attribute"
      pattern: ".provider-fields\\[hidden\\]"
---

<objective>
Append Phase 3 CSS rules to setup/static/wizard.css. Adds select styling, conditional provider field group hiding, three-column row layout, and inbox row container and button styles.

Purpose: hosting.html and inboxes.html reference CSS classes that do not yet exist. Without these rules, the hosting step layout is broken and the inbox row manager is unstyled.
Output: wizard.css with 10 new rule blocks appended after line 265; no existing rules modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hosting-provider-inbox-manager/03-UI-SPEC.md

<interfaces>
<!-- Current wizard.css ends at line 265 with: -->
.field-row:has(input[type="checkbox"]) label {
    margin-bottom: 0;
    font-weight: 400;
}
<!-- Append after this line only. Do NOT edit any line above it. -->

<!-- Exact CSS to append (from UI-SPEC CSS Additions section): -->
/*
select {
    width: 100%;
    padding: 8px 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    font-size: 16px;
    font-family: inherit;
    background-color: #ffffff;
    color: #111827;
    cursor: pointer;
}
select:focus {
    outline: 2px solid #2563eb;
    outline-offset: 2px;
    border-color: #2563eb;
}
.provider-fields[hidden] { display: none; }
.field-row-thirds {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 16px;
}
.inbox-row {
    background-color: #f3f4f6;
    border: 1px solid #d1d5db;
    border-radius: 8px;
    padding: 16px;
    margin-bottom: 16px;
}
.inbox-row-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
}
.inbox-row-label {
    font-size: 14px;
    font-weight: 600;
    color: #374151;
}
.remove-inbox {
    min-height: 44px;
    padding: 0 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    background-color: #ffffff;
    color: #dc2626;
    font-size: 14px;
    cursor: pointer;
    white-space: nowrap;
    flex-shrink: 0;
}
.remove-inbox:disabled { color: #d1d5db; cursor: not-allowed; }
#add-inbox {
    font-size: 14px;
    color: #2563eb;
    background: none;
    border: none;
    padding: 4px 0;
    cursor: pointer;
    text-decoration: underline;
    margin-bottom: 16px;
}
*/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 3 CSS rules to wizard.css</name>
  <read_first>
    - setup/static/wizard.css (read full file to confirm current end state and line count — confirm line 265 is the last line with content)
    - .planning/phases/03-hosting-provider-inbox-manager/03-UI-SPEC.md (CSS Additions section — use as the authoritative source for exact property values)
  </read_first>
  <action>
Open setup/static/wizard.css and append the following block at the very end of the file (after the last existing rule). Do NOT modify any existing line.

Append exactly this content:

```css

/* ── Phase 3 additions ────────────────────────────────────────────────────── */

/* Provider dropdown — matches input appearance */
select {
    width: 100%;
    padding: 8px 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    font-size: 16px;
    font-family: inherit;
    background-color: #ffffff;
    color: #111827;
    cursor: pointer;
}
select:focus {
    outline: 2px solid #2563eb;
    outline-offset: 2px;
    border-color: #2563eb;
}

/* Conditional provider field groups */
.provider-fields[hidden] { display: none; }

/* Three-column split row (inbox row 2: site name + URL + base path) */
.field-row-thirds {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 16px;
}

/* Inbox row container */
.inbox-row {
    background-color: #f3f4f6;
    border: 1px solid #d1d5db;
    border-radius: 8px;
    padding: 16px;
    margin-bottom: 16px;
}

/* Inbox row header (label + remove button) */
.inbox-row-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
}
.inbox-row-label {
    font-size: 14px;
    font-weight: 600;
    color: #374151;
}

/* Remove inbox button — matches remove-sender */
.remove-inbox {
    min-height: 44px;
    padding: 0 12px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    background-color: #ffffff;
    color: #dc2626;
    font-size: 14px;
    cursor: pointer;
    white-space: nowrap;
    flex-shrink: 0;
}
.remove-inbox:disabled { color: #d1d5db; cursor: not-allowed; }

/* Add inbox button — matches add-sender */
#add-inbox {
    font-size: 14px;
    color: #2563eb;
    background: none;
    border: none;
    padding: 4px 0;
    cursor: pointer;
    text-decoration: underline;
    margin-bottom: 16px;
}
```

After appending, verify:
1. The file still contains all original rules (line count >= 265)
2. The new rules are at the bottom
3. No existing rule was altered
  </action>
  <acceptance_criteria>
    - grep -c "provider-fields\[hidden\]" wizard.css returns 1
    - grep -c "field-row-thirds" wizard.css returns 1
    - grep -c "inbox-row" wizard.css returns at least 3 (.inbox-row, .inbox-row-header, .inbox-row-label)
    - grep -c "remove-inbox" wizard.css returns 2 (.remove-inbox and .remove-inbox:disabled)
    - grep -c "add-inbox" wizard.css returns 1
    - grep -c "remove-sender" wizard.css returns 2 (existing rules unchanged)
    - wc -l wizard.css shows line count > 265
  </acceptance_criteria>
  <verify>
    <automated>grep -c "provider-fields\[hidden\]\|field-row-thirds\|remove-inbox\|add-inbox" /Users/home/Projects/thoughts-to-platform-builder/setup/static/wizard.css</automated>
  </verify>
  <done>
    - wizard.css has all Phase 3 CSS rules appended
    - .provider-fields[hidden] { display: none; } present
    - .field-row-thirds with 3-column grid present
    - .inbox-row, .inbox-row-header, .inbox-row-label styles present
    - .remove-inbox and .remove-inbox:disabled present
    - #add-inbox styles present
    - No existing CSS rule was modified (grep for existing rules like .remove-sender still returns same count)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file | Static asset served by Flask; read-only at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | CSS file modification | accept | Static file served locally; no runtime modification possible |
</threat_model>

<verification>
# Phase 3 rules present
grep -n "Phase 3\|provider-fields\|field-row-thirds\|inbox-row\|remove-inbox\|add-inbox" /Users/home/Projects/thoughts-to-platform-builder/setup/static/wizard.css

# Existing Phase 2 rules intact
grep -c "remove-sender\|toggle-visibility\|field-row-split" /Users/home/Projects/thoughts-to-platform-builder/setup/static/wizard.css
# Expected: same counts as before (2, 1, 1 respectively)
</verification>

<success_criteria>
- wizard.css contains all 10 new Phase 3 rule blocks
- File line count increased from 265 to ~340
- All property values match UI-SPEC exactly (colors, sizes, border-radius)
- No existing rule block was modified
</success_criteria>

<output>
After completion, create `.planning/phases/03-hosting-provider-inbox-manager/03-06-SUMMARY.md`
</output>
