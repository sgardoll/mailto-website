---
phase: 18-integrate-orchestrator
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/workflow_engine/validator.py
  - apps/workflow_engine/tests/test_validator.py
autonomous: true
requirements: [SEC-01]
tags: [validator, security, alpine, xss]

must_haves:
  truths:
    - "Any generated module containing the x-html attribute is rejected by validate_module"
    - "Modules that use only x-text (no x-html) continue to pass validation"
    - "The new check runs alongside existing VAL-01..VAL-07 checks without regressing them"
  artifacts:
    - path: "apps/workflow_engine/validator.py"
      provides: "x-html attribute detection in _Inspector + SEC-01 error append in validate_module"
      contains: "has_x_html"
    - path: "apps/workflow_engine/tests/test_validator.py"
      provides: "RED test for x-html ban, GREEN once implemented"
      contains: "x-html"
  key_links:
    - from: "validator.py:_Inspector.handle_starttag"
      to: "validator.py:validate_module errors list"
      via: "self.has_x_html flag"
      pattern: "has_x_html"
---

<objective>
Add SEC-01 (x-html directive ban) to validator.py. Any generated Alpine module containing the `x-html` attribute must be rejected with a clear error message. This rule was explicitly deferred from Phase 17 and must land in Phase 18 despite CONTEXT.md's "no validator changes" note (see RESEARCH Finding 3).

Purpose: Prevent Alpine.js `x-html` injection XSS in LLM-generated modules. `x-html` renders raw HTML from scope expressions; `x-text` (text-only) is the only safe LLM-insertion directive.

Output: validator.py rejects x-html; test_validator.py has a dedicated test case covering the ban.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-integrate-orchestrator/18-CONTEXT.md
@.planning/phases/18-integrate-orchestrator/18-RESEARCH.md
@.planning/phases/18-integrate-orchestrator/18-PATTERNS.md

<interfaces>
<!-- Current _Inspector fields (validator.py lines 54-62) -->
```python
class _Inspector(HTMLParser):
    def __init__(self) -> None:
        super().__init__(convert_charrefs=True)
        self.has_x_data: bool = False
        self.has_event_handler: bool = False
        self.x_if_on_div: bool = False
        self.cdn_src_urls: list[str] = []
        self.script_texts: list[str] = []
        self._in_script: bool = False
        self._script_buf: list[str] = []
```

<!-- Current x-if-on-div check pattern (validator.py handle_starttag, line ~68) -->
```python
if tag == "div" and "x-if" in attr_dict:
    self.x_if_on_div = True
```

<!-- Current VAL-04 error append pattern in validate_module (line ~131) -->
```python
if inspector.x_if_on_div:
    errors.append("x-if used on <div> element (must be on <template>)")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing test for x-html ban</name>
  <files>apps/workflow_engine/tests/test_validator.py</files>
  <read_first>
    - apps/workflow_engine/tests/test_validator.py (to see existing test style and fixtures)
    - apps/workflow_engine/validator.py (to confirm `has_x_html` is NOT yet present)
  </read_first>
  <behavior>
    - Test `test_x_html_directive_rejected` creates a minimal valid Alpine module HTML string but adds a single `<div x-html="foo">` element
    - Calling `validate_module(html_b64)` returns a result where `ok is False` and the errors list contains a string matching the substring `"x-html"`
    - Test `test_x_text_allowed` confirms modules using only `x-text` still validate as `ok is True` (regression guard)
  </behavior>
  <action>
    Open apps/workflow_engine/tests/test_validator.py. Locate the existing x-if-on-div test (search for `x_if_on_div` or `x-if`) and mirror its fixture style. Add TWO new tests:

    1. `test_x_html_directive_rejected`:
       - Build a minimal-valid Alpine HTML string (copy the known-good HTML fixture used by existing tests — likely a `_MIN_VALID_HTML` constant or inline f-string; match whatever the existing tests use).
       - Inject one extra element: `<div x-html="foo"></div>` inside the `x-data` root.
       - Base64-encode (use `base64.b64encode(html.encode()).decode()`).
       - Call `validate_module(html_b64)`; assert `result.ok is False`.
       - Assert `any("x-html" in err for err in result.errors)`.

    2. `test_x_text_allowed`:
       - Use the same minimal-valid HTML but add `<span x-text="foo"></span>`.
       - Assert `result.ok is True` (no x-html regression).

    Run `pytest apps/workflow_engine/tests/test_validator.py::test_x_html_directive_rejected -x` — test MUST FAIL (the field `has_x_html` does not exist yet, so the error is never appended). This is the RED step.

    Commit: `test(18-01): RED — add failing test for x-html ban (SEC-01)`
  </action>
  <verify>
    <automated>cd /Users/home/Projects/thoughts-to-platform-builder && python -m pytest apps/workflow_engine/tests/test_validator.py::test_x_html_directive_rejected -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def test_x_html_directive_rejected" apps/workflow_engine/tests/test_validator.py
    - grep -q "def test_x_text_allowed" apps/workflow_engine/tests/test_validator.py
    - grep -q 'x-html="foo"' apps/workflow_engine/tests/test_validator.py
    - pytest exit code for `test_x_html_directive_rejected` is NON-ZERO (RED)
    - git log -1 --format=%s contains "test(18-01)"
  </acceptance_criteria>
  <done>Two new tests exist; `test_x_html_directive_rejected` fails against current validator.py; commit created with `test(18-01):` prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement x-html detection + error append</name>
  <files>apps/workflow_engine/validator.py</files>
  <read_first>
    - apps/workflow_engine/validator.py (full file — need to see _Inspector.__init__, handle_starttag, and validate_module)
    - apps/workflow_engine/tests/test_validator.py (to understand what `validate_module` is expected to return)
  </read_first>
  <behavior>
    - `_Inspector.has_x_html: bool = False` initialized in `__init__`
    - When handle_starttag sees any tag with an `x-html` attribute, `self.has_x_html` becomes True
    - `validate_module` appends `"x-html directive is banned (use x-text for LLM-inserted content)"` to errors when `inspector.has_x_html` is True
    - New check runs AFTER the VAL-04 x-if-on-div check and BEFORE the VAL-02 Alpine CDN check (per PATTERNS.md placement note)
  </behavior>
  <action>
    Edit apps/workflow_engine/validator.py in three places:

    1. In `_Inspector.__init__` (around line 54-62), add a new field after `self._script_buf`:
       ```python
       self.has_x_html: bool = False
       ```

    2. In `_Inspector.handle_starttag` (around line 64-77), immediately after the existing x-if-on-div check:
       ```python
       if tag == "div" and "x-if" in attr_dict:
           self.x_if_on_div = True
       # ADD:
       if "x-html" in attr_dict:
           self.has_x_html = True
       ```
       (Check applies to ANY tag, not just div — Alpine's x-html is valid on any element.)

    3. In `validate_module` (around line 120-149), immediately after the VAL-04 x-if check and BEFORE the VAL-02 Alpine CDN check:
       ```python
       if inspector.x_if_on_div:
           errors.append("x-if used on <div> element (must be on <template>)")
       # ADD:
       if inspector.has_x_html:
           errors.append("x-html directive is banned (use x-text for LLM-inserted content)")
       ```

    Run `pytest apps/workflow_engine/tests/test_validator.py -x` — ALL tests must pass (GREEN).

    Commit: `feat(18-01): GREEN — ban x-html directive in validator (SEC-01)`
  </action>
  <verify>
    <automated>cd /Users/home/Projects/thoughts-to-platform-builder && python -m pytest apps/workflow_engine/tests/test_validator.py -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "self.has_x_html: bool = False" apps/workflow_engine/validator.py
    - grep -q '"x-html" in attr_dict' apps/workflow_engine/validator.py
    - grep -q "x-html directive is banned" apps/workflow_engine/validator.py
    - pytest exit code for entire `test_validator.py` is 0 (all tests pass)
    - git log -1 --format=%s contains "feat(18-01)"
  </acceptance_criteria>
  <done>x-html is detected and rejected; all validator tests pass; SEC-01 is implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → validator | Untrusted generated HTML crosses into validator for gate-keeping |
| Validator → integrate | Only validator-approved HTML may be written to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `_Inspector.handle_starttag` | mitigate | Detect `x-html` attribute on any tag; surface as validator error per SEC-01 |
| T-18-02 | Elevation of Privilege | LLM-generated Alpine module | mitigate | `x-html` ban prevents arbitrary HTML injection within iframe scope (iframe sandbox is defense-in-depth, not sole control) |
| T-18-03 | Tampering | `attr_dict` lookup | accept | `dict(attrs)` loses duplicate attributes; HTML spec disallows duplicate attribute names so impact is negligible |
</threat_model>

<verification>
- `pytest apps/workflow_engine/tests/test_validator.py -x` passes with 0 failures
- `grep -n "x-html" apps/workflow_engine/validator.py` shows the attribute check AND the error string
- A manual base64 encode of an HTML snippet containing `x-html="x"` passed to `validate_module` returns `ok=False` with the SEC-01 error message
</verification>

<success_criteria>
- validator.py rejects any module containing `x-html` on any element
- All existing validator tests still pass (no regression to VAL-01..VAL-07)
- SEC-01 requirement ID is closed
- Two new tests live in test_validator.py covering the positive (x-text allowed) and negative (x-html rejected) cases
</success_criteria>

<output>
After completion, create `.planning/phases/18-integrate-orchestrator/18-01-SUMMARY.md`
</output>
