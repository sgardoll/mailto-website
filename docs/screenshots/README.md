# Screenshots referenced from project READMEs

Drop the following files here — the README references each by name.
GitHub renders the README at any size; aim for high-DPI captures
(retina or 2x equivalent) and let the markdown shrink them with `width=`.

| File | What to capture | Suggested aspect |
|---|---|---|
| `hero-banner.png` | Project hero — wordmark + tagline + a wide screenshot collage. Optional but high-impact. | 16:5 wide |
| `live-site.png` | A real evolved site (e.g. `platform.connectio.com.au/it/`) with multiple thread cards. | 16:9 |
| `wizard-stepper.png` | Just the stepper at the top of the wizard, mid-flow with a couple of completed steps. | 16:5 wide crop |
| `wizard-hosting.png` | Hosting step with SiteGround selected and SSH key picker visible. | 4:3 |
| `wizard-done.png` | Done screen with deploy panel mid-deploy and the new "Add to Contacts" buttons. | 4:3 |
| `thread-page.png` | A thread detail page that has multiple folded entries. | 16:9 |
| `health-check.png` | The `/health` JSON in a terminal or browser tab. | 16:9 narrow |
| `template-rendered.png` | Two rendered template instances side-by-side (different topics, same template). | 16:9 |

Until these exist the README will show broken-image icons in place — nothing else breaks.
