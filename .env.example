# Source these into your shell (or use a .env loader) before running the workflow.
# Referenced from workflow/config.yaml via ${VAR} substitution.

# Gmail app password (NOT your normal password).
# https://myaccount.google.com/apppasswords
GMAIL_APP_PASSWORD=
